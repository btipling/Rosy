#include "scene_one.h"
#include "../../utils/utils.h"


inline rh::result scene_one::build(rh::ctx ctx)
{
	rosy_utils::debug_print_a("building scene one!\n");
	return rh::result::ok;
}

inline rh::result scene_one::draw(rh::ctx ctx)
{
	rosy_utils::debug_print_a("drawing scene one!\n");
	return rh::result::ok;
}

